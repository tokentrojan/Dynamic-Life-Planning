import { collection, getDocs, updateDoc, doc } from 'firebase/firestore';
import { db } from '../firebase';
import { tasks } from '../data/firebasetasks';


export const parentChild = async (userID: string) => {
    
}

